const { ToggleScript, library } = require("../../lib/ohts-Lib");

ToggleScript("FireWork Verify",async()=>{
    Client.waitTick(1);
    if(Hud.getOpenScreenName() === "3 Row Chest" || Hud.getOpenScreenName() === "6 Row Chest"){
        
    }
    
})